name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Apply source-driven CLI README implementation
        if: github.head_ref == 'agent/source-driven-cli-readme'
        run: |
          node .github/scripts/apply-source-driven-cli.mjs
          rm -f .github/scripts/apply-source-driven-cli.mjs

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.13'

      - name: Install dependencies
        run: bun install

      - name: Format implementation
        if: github.head_ref == 'agent/source-driven-cli-readme'
        run: bun run format

      - name: Run build
        run: bun run build

      - name: Capture lint output
        run: |
          set +e
          bun run lint > lint-output.txt 2>&1
          echo $? > lint-exit-code.txt
          cat lint-output.txt
          exit 0

      - name: Upload lint output
        uses: actions/upload-artifact@v4
        with:
          name: lint-output
          path: lint-output.txt

      - name: Fail on lint errors
        run: test "$(cat lint-exit-code.txt)" = "0"
